from machinepack.pack import MockResult
from machinepack.pack import MachineResponse

tests = [
    {
      'description': '',
      'inputs': {'': ''},
      'exit': MachineResponse('success'),
    }
]