# from .appliances import ApplianceFactory
#
# class HouseholdFactory(object):
#     """A thing to make household instances using a given scenario
#     """
#     def __init__(self, path=None):
#         # path=None, freq="1Min", method="cubic"
#         self.appliance_factory = ApplianceFactory(**kwargs)
#
#     def __call__(self, appliance, cycle_length):
#         model = self.model_factory(appliance)
#         return Appliance(model, cycle_length)
