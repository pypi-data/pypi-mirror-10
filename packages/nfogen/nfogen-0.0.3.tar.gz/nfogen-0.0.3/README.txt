Installation:
git clone https://github.com/borisbabic/nfogen.git
sudo pip install nfogen/ #don't forget the /

or 

sudo pip install nfogen #without the /

Usage:
nfogen directory

What it does:

Walks through subdirectories in directory and creates a tvshow.nfo with the url to tvdb.
The user is prompted for which show to use
