============
Installation
============

At the command line::

    $ easy_install magic_lightning_remote

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv magic_lightning_remote
    $ pip install magic_lightning_remote
