============
Installation
============

At the command line::

    $ easy_install decisionTable

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv decisionTable
    $ pip install decisionTable
