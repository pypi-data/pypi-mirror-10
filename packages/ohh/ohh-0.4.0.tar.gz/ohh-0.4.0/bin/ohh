#!/usr/bin/env python

from ohh import cli, Runner, OhhError

if __name__ == '__main__':
    args = vars(cli.parse())
    action, basedir = args.pop('action'), args.pop('basedir')
    runner = Runner(basedir)
    try:
        runner.run(action, **args)
    except OhhError as e:
        print("error: " + e.message)
