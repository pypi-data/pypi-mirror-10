AGGREGATOR='timeseries'
TOTAL_LENGTH=LENGTH
DEFAULTS={'PLOT': None}
DESCRIPTION="TEST"

DATA_SETS = o([
('FIRST', {'command': '/bin/sh -c "while true; do date; sleep 1; done"', 'runner': 'silent_process', 'kill_after': 'THIRD'}),
('SECOND', {'command': '/bin/sh -c "date"', 'runner': 'silent_process', 'run_after': 'FIRST'}),
('THIRD', {'runner': 'timer', 'timeout': 5, 'kill_after': 'FOURTH'}),
('FOURTH', {'runner': 'timer', 'timeout': 3})
])
