AGGREGATOR='timeseries'
TOTAL_LENGTH=LENGTH
DEFAULTS={'PLOT': None}
DESCRIPTION="TEST"

DATA_SETS = o([
('FIRST', {'command': '/bin/sh -c "date; sleep 5"', 'runner': 'silent_process'}),
('SECOND', {'command': '/bin/sh -c "date"', 'runner': 'silent_process', 'after':'FIRST'}),
])
