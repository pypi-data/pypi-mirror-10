============
Installation
============

At the command line::

    $ easy_install vld

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv vld
    $ pip install vld

Or, with pipsi::

    $ pip install vld
